// create an array variable containing the names
let stLouisSports = ["Cardinals", "Blues", "City FC", "BattleHawks"]
for (i=0; i<stLouisSports.length; i++) {
    console.log(stLouisSports[i])
}
// write a for loop that prints each name on a different line
